<nav class="navbar navbar-light border-bottom" style="background-color: #fff8ee;">
    <div class="container" style="max-width: 900px;">

        <!-- Left: Avatar + Brand -->
        <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <img src="/images/avatar.png"
                 class="rounded-circle"
                 width="52" height="52"
                 alt="Brands Advisory" />
            <div>
                <div class="fw-bold text-dark" style="font-size: 1.25rem; line-height: 1.2;">Brands Advisory</div>
                <div class="text-secondary" style="font-size: 0.9rem; line-height: 1.2;">Freelance IT Consulting</div>
            </div>
        </a>

        <!-- Center: Navigation tabs -->
        <div class="d-flex gap-1">
            <NavLink href="/" Match="NavLinkMatch.All"
                     class="btn btn-sm rounded-pill px-3"
                     ActiveClass="btn-secondary"
                     InactiveClass="btn-outline-secondary border-0 text-secondary">
                Start
            </NavLink>
            <NavLink href="/coach"
                     class="btn btn-sm rounded-pill px-3"
                     ActiveClass="btn-secondary"
                     InactiveClass="btn-outline-secondary border-0 text-secondary">
                Coach
            </NavLink>
            <NavLink href="/blog"
                     class="btn btn-sm rounded-pill px-3"
                     ActiveClass="btn-secondary"
                     InactiveClass="btn-outline-secondary border-0 text-secondary">
                Blog
            </NavLink>
        </div>

        <!-- Right: Login/Logout -->
        <div class="d-flex align-items-center">
            <AuthorizeView>
                <Authorized>
                    <a href="/logout"
                       class="btn btn-outline-secondary btn-sm">
                        Logout
                    </a>
                </Authorized>
                <NotAuthorized>
                    <a href="/login"
                       class="btn btn-outline-secondary btn-sm">
                        Login
                    </a>
                </NotAuthorized>
            </AuthorizeView>
        </div>

    </div>
</nav>

